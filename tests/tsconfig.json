{
  /**
   * Цей файл розширює основний конфіг `tsconfig.vitest.json`.
   * Він потрібен для того, щоб TypeScript та редактори коду (напр., VS Code)
   * правильно розпізнавали шляхи та налаштування для файлів у директорії `tests`.
   */
  "extends": "../tsconfig.vitest.json"
}
