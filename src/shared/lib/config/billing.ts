/**
 * Конфігурація бізнес-правил, пов'язаних з білінгом, тарифами та запрошеннями.
 */

// ============================================================================
// КОНФІГУРАЦІЯ ТАРИФІВ
// ============================================================================

/**
 * Типи сутностей, для яких застосовуються квоти.
 * Є єдиним джерелом правди для сервісів, що перевіряють ліміти.
 */
export type QuotaEntity = "contacts" | "deals" | "users";

/**
 * Константи, що визначають ліміти для кожного тарифного плану.
 * Це єдине "джерело правди" для всіх бізнес-правил, пов'язаних з квотами.
 */
export const WORKSPACE_TIER_LIMITS = {
  free: {
    users: 2,
    contacts: 100,
    deals: 50,
    storage_mb: 500,
    workspaces: 2, // Ліміт на кількість воркспейсів
  },
  starter: {
    users: 5,
    contacts: 5000,
    deals: 1000,
    storage_mb: 5120, // 5 GB
    workspaces: 5,
  },
  pro: {
    users: 20,
    contacts: 50000,
    deals: 10000,
    storage_mb: 51200, // 50 GB
    workspaces: 20,
  },
  enterprise: {
    users: 999999,
    contacts: 999999,
    deals: 999999,
    storage_mb: 999999,
    workspaces: 999999,
  },
};

// ============================================================================
// КОНФІГУРАЦІЯ ЗАПРОШЕНЬ
// ============================================================================

/**
 * Термін дії запрошення за замовчуванням (7 днів)
 */
export const DEFAULT_INVITATION_EXPIRY_DAYS = 7;
